import "./RegisterPanel.css"

interface Props {

}

const RegisterPanel: React.FC<Props> = (): JSX.Element => {
    return (
        <div>
            RegisterPanel
        </div>
    );
}

export default RegisterPanel